export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Mission Dashboard</h1>
      <p className="mt-2 text-sm text-zinc-500">
        Portfolio grid — phase, gate status, open risks by domain, SoA
        completeness — lands in M5.
      </p>
    </div>
  );
}
