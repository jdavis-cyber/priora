export default function ControlsPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Controls / SoA</h1>
      <p className="mt-2 text-sm text-zinc-500">
        Control library (ISO 42001 Annex A + selected 800-53) and per-project
        Statement of Applicability land in M3 (Registers).
      </p>
    </div>
  );
}
