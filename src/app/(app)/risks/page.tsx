export default function RisksPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Risks</h1>
      <p className="mt-2 text-sm text-zinc-500">
        Risk register — 7-domain taxonomy, 3×3 likelihood × impact — lands in
        M3 (Registers).
      </p>
    </div>
  );
}
