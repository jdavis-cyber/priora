export default function AuditLogPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Audit Log</h1>
      <p className="mt-2 text-sm text-zinc-500">
        Append-only accountability trail (enforced at the database privilege
        layer since M1). The browsing view lands with the registers in M3–M5.
      </p>
    </div>
  );
}
